interface:
  display_name: "Multorum Worker"
  short_description: "Operate inside one Multorum worker worktree safely"
  default_prompt: "Use $multorum-worker to execute a task inside one Multorum worker worktree through `multorum local ...` or the worker MCP surface."
