[package]
name = "multorum"
version = "0.0.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
thiserror = "2"
toml = "1.0"
tracing = "0.1"
wax = "0.7"

[dev-dependencies]
tempfile = "3"
