[package]
name = "multorum"
version = "0.0.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
toml = "1.0"
tracing = "0.1"
walkdir = "2"
wax = "0.7"

[dev-dependencies]
tempfile = "3"
